node_modules/
package-lock.json
package.json

# generated single-file builds — index.html is the build output that matters,
# these two re-inline every painting as base64 and are ~3MB of duplicated assets
artifact.html
preview.html
